#!/usr/bin/env bash
export FPVM_HOME=$(realpath "$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )")
export FPVM_WRAP=reverse
export PATH=$FPVM_HOME/analysis:$FPVM_HOME/scripts:$FPVM_HOME/analysis/dep/e9patch:$PATH

